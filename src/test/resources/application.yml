spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test
            client-secret: test
            scope:
              - profile
              - email
jwt:
  secret: test
redirect:
  baseUri: baseUri
  addNickNameUri : nickNameUri